{
  "mcpServers": {
    "figma-developer": {
      "command": "figma-developer-mcp",
      "args": ["--stdio"],
      "env": {
        "NODE_ENV": "production",
        "FIGMA_API_KEY": "${FIGMA_API_KEY}"
      }
    }
  }
}